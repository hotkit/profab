#!/usr/bin/env python
import fabric.network
import logging
import sys

from profab.server import Server


def main(client, hostname):
    logging.basicConfig(level=logging.INFO)
    Server.connect(client, hostname)


if __name__=="__main__":
    try:
        main(*sys.argv[1:])
    finally:
        fabric.network.disconnect_all()
